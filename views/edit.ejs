<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ENIssues</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <header class="bg-gray-900 rounded-lg shadow-sm m-4 dark:bg-gray-800">
        <h1 class="mb-4 text-4xl font-extrabold leading-none tracking-tight text-white md:text-5xl lg:text-6xl dark:text-white text-center">ENIssues</h1>
    </header>

<form action="/edit-issue/<%=issue.id%>" method="post">
    <div class="m-3">
        <label for="titre">Titre:</label>
        <input type="text" name="titre" id="titre" class="shadow-md border border-gray-300 m-2 p-1" value="<%= issue.titre %>">
    </div>

    <div class="m-3">
        <label for="description">Description:</label>
        <textarea name="description" id="description" class="shadow-md border border-gray-300 m-2 p-1"> <%= issue.description %> </textarea>
    </div>

    <div class="m-3">
        <label for="auteur">Auteur:</label>
        <input type="text" name="auteur" id="auteur" class="shadow-md border border-gray-300 m-2 p-1" value="<%= issue.auteur %>">
    </div>

    <div class="m-3">
        <label for="date">Date:</label>
        <input type="date" name="date" id="date" class="shadow-md border border-gray-300 m-2 p-1" value="<%= issue.date %>">
    </div>

    <div class="m-3">
        <label for="etat">Etat:</label>
        <% if (issue.etat == 'en-cours') { %>
            <select name="etat" id="etat" class="shadow-md border border-gray-300 m-2 p-1">
                <option value="en-cours" selected>En cours</option>
                <option value="resolu">Résolu</option>
            </select>
        <% } else { %> 
            <select name="etat" id="etat" class="shadow-md border border-gray-300 m-2 p-1">
                <option value="en-cours">En cours</option>
                <option value="resolu" selected>Résolu</option>
            </select>
        <% } %>
    </div>

    <div class="m-3">
        <button 
            type="submit" 
            class="m-2 text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800"
        >
            Modifier
        </button>
    </div>
</form>

    <footer class="bg-gray-900 text-white rounded-lg shadow-sm m-4 dark:bg-gray-800 text-center m-2">
        &copy; ENI TOP GROUP Y-A-A
    </footer>
</body>
</html>